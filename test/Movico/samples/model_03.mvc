model Expr {}

model Int : Expr {
    (+) : Int -> Int
}

model Add : Expr {
    _1 : Expr
    _2 : Expr
}

class ExprEval this:Expr {
    eval : Int
} {
    def (Int).eval = this
    def (Add).eval = (self this._1).eval + (self this._2).eval
}
