model Expr {}

model Int : Expr {
    value : int
}

model Add : Expr {
    left : Expr
    right : Expr
}

class ExprEval this:Expr {
    eval : int
} {
    def (Int).eval = this.value
    def (Add).eval = this.left + this.right
}
